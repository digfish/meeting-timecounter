{
  "name": "meeting-timecounter",
  "version": "0.0.1",
  "description": "Meeting time counter",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node server.js",
    "dev": "nodemon -V  -w ./server.js",
    "browser": "open-cli http://localhost:3000",
    "start": "start npm run browser && npm run server",
    "ws": "node ./ws.js"
  },
  "keywords": [
    "timecounter",
    "browser"
  ],
  "author": "digfish",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "node-localstorage": "^3.0.5",
    "ws": "^8.14.2"
  }
}
